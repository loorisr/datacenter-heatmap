[project]
name = "datacenter-heatmap"
version = "0.1.0"
description = "Generate datacenter location heatmap"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "folium>=0.20.0",
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
]
